# # Map for WebSocket upgrade
# map $http_upgrade $connection_upgrade {
#     default upgrade;
#     '' close;
# }

# # Map incoming ports 3000-3999 to backend ports 3000-3999
# map $server_port $backend_port {
#     ~^3([0-9]{3})$ 3$1;
# }

# server {
#     listen 3000-3999;
    
#     location / {
#         # Proxy to same port on localhost
#         proxy_pass http://127.0.0.1:$backend_port;
        
#         # WebSocket support
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection $connection_upgrade;
        
#         # Standard proxy headers
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
        
#         # Timeouts for long-lived connections
#         proxy_connect_timeout 7d;
#         proxy_send_timeout 7d;
#         proxy_read_timeout 7d;
#     }
# }

server {
    listen 80;
    server_name localhost;
    
    # API endpoint on port 8080
    location /get {
        proxy_pass http://127.0.0.1:8080/get;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Only allow GET method
        limit_except GET {
            deny all;
        }
    }
    
    location /kill {
        proxy_pass http://127.0.0.1:8080/kill;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Only allow GET method
        limit_except GET {
            deny all;
        }
    }
    
    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}